# Railway release — 2026-09-14

## Published, not inferred from Git

Project: agent-runtime-console, production. Only these two services were changed:

| Service | Application source | Version | Successful deployment |
| --- | --- | --- | --- |
| langgraph-simple-agent-clean | 198acd0 | 0.2.0 | d7a4d82d-46e6-4007-8073-28e084ee5f62 |
| agent-chat-ui-fork | 524cfe1 | 0.1.0 | 4e662b1a-1927-47cc-aeae-5f9b8b4be0c2 |

Uploaded tested worktrees directly to the exact services. PRs remain open:
[backend #6](https://github.com/markanthony91/simple-agent-template/pull/6),
[frontend #5](https://github.com/markanthony91/agent-chat-ui/pull/5).
Application source is unchanged from those commits; backend runbook documentation
was amended during rollout. A main merge would also trigger the older duplicate
backend service, so it was not performed implicitly. Coordinate Git integration
before the next main autodeploy, which could replace this release.

## Recovery and startup

- Backed up /data, ephemeral files and API exports of 3 assistants, 1 conversation
  and 21 checkpoints. Archive checksum matched the protected local copy:
  73f0af1cb94eade48cb93adeab48d1b7aee611e2ca0d19f9c85f7a6970726f0e.
- Volume archive: /data/backups/pre-runtime-020-20260914T215115Z.tar.gz.
  No backup data or credentials are committed.
- Railway's custom start command bypassed the image ENTRYPOINT. An ordinary
  redeploy reused old settings; a fresh upload applied the new command.
- Startup now uses exec, the persistence preparation module and --no-reload.
  RAILWAY_DEPLOYMENT_DRAINING_SECONDS=30, healthcheck /info. Confirmed live
  /app/.langgraph_api -> /data/langgraph, one replica and preserved volume.
- Restored all 16 original messages with content/IDs checked against the export.
  Reconstructed checkpoint IDs/timestamps differ; originals remain in backup.
  Restoration does not execute historical tools or LLM calls.
- Initial restart check exposed missing graceful checkpoint flush. After changing
  exec/no-reload/draining, a second controlled restart preserved the original 16
  messages and new 4-message smoke. Shutdown/startup logs at 22:06:56Z / 22:07:03Z
  corroborate the restart.

## Published validation

- Frontend HTTP 200; backend /info HTTP 200, langgraph-api 0.14.0 / langgraph 1.2.6.
- Both package versions verified inside their containers.
- Real Qwen tool loop: utc_now -> tool result -> final response; finish reasons
  tool_calls/stop. No customer lookup or financial action used in this smoke.
- Published Chromium UI: no JavaScript errors; reload preserved reply.
  First visible text 3863 ms, total 4672 ms (UTC tool scenario, not generic TTFT).
- Follow-up Qwen index lookup: okf_index, successful result, 2594 ms total.
- Final startup configuration repeated Qwen UTC test: 2763 ms total; its four
  messages survived the controlled restart.
- Local regression baseline: 79 tests, 81.85% coverage, 9 browser tests. Production
  timings are not comparable to the synthetic 50-ms chunk fixture.

## Limits / next work

- Full financial Qwen journeys, authorized policy metadata and fidelity review
  remain pending. Synthetic journeys do not prove real-model behavior.
- Single-replica in-memory lab, not crash-safe production storage. Graceful-restart
  success does not prove resistance to SIGKILL/host failure.
- Existing anonymous operator access remains; use synthetic customer data only.
- No output Evidence Gate added; visible text can contain model errors.
- Original console, Postgres, bridge, Lovable, WhatsApp and duplicate Railway
  services were not deployed or reconfigured.

Reference: [Railway graceful draining](https://docs.railway.com/deployments/deployment-teardown).
