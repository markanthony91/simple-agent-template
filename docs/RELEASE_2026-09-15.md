# Published rollout — 2026-09-15

Only the existing Railway runtime/chat pair was deployed. No changes to Lovable,
WhatsApp, the original console, inference host, or published OKF bundle.

| Service | Version / source | Successful deployment |
| --- | --- | --- |
| langgraph-simple-agent-clean | 0.2.2 / 170137b | b3a6382b-070c-4bca-b7d2-e0e745a0ae88 |
| agent-chat-ui-fork | 0.1.1 / e44769f | e00a7b4f-4d98-4cb0-88bf-9f4ba8251247 |

37 backend source/config files matched local SHA256 values inside the running
container. Package versions and persistent /app/.langgraph_api -> /data/langgraph
were verified directly. PRs remain stacked: backend #7, frontend #6. Main was not
merged because it also targets older duplicate services; coordinate integration.

## Shared chat and model

[Open the chat](https://agent-chat-ui-fork-production.up.railway.app/).
The existing NEXT_PUBLIC_API_URL and NEXT_PUBLIC_ASSISTANT_ID defaults were set
before building. A clean browser opens the root without a connection form,
query parameters, login setup or stored API key. Share no threadId.

LLM_MODEL remains server-pinned to Qwen/Qwen3-30B-A3B-Instruct-2507-FP8. A new
protocol regression proves configurable.model/model_name cannot replace it.
No model-selection library, public LLM endpoint/key or guest-auth system added.
This is still an anonymous operator lab, not a restricted guest account. Only
authorized testers and synthetic data; model pinning does not restrict admin access.

## Backup and build recovery

Backup: /data/backups/pre-runtime-021-20260915T154311Z (API exports + volume archive).
Archive SHA256: 18422f4b67544be33c109a80e46802496a2a2d503a9303bf4d3d06d8c5033d58.
All six previous conversation message hashes and 3,228 data-file hashes matched
after deployment, before smoke tests. The backup is on-volume, not disaster recovery.

Initial backend deployment 2b7600b7-8ed3-48ea-81ac-e3505619124a failed in pip
bootstrap because it inherited an unavailable runtime proxy. No new backend
started from that build. 0.2.2 excludes proxy variables during pip bootstrap,
as uv sync already did. Runtime proxy configuration is unchanged. Docker build
also passed locally with deliberately invalid HTTP_PROXY/HTTPS_PROXY build args.

## Validation and limits

- 101 pytest pass, one skipped, 83% coverage; Ruff passes.
- Typecheck/build pass; ESLint zero errors, 24 existing warnings.
- Published Chromium E2E passed: direct root, actual Qwen metadata, utc_now,
  okf_index, post-stream audit, consecutive turns, reload/history, zero JS errors.
  Layouts 1440x900, 1024x768 and 390x844 had no horizontal overflow; mobile reviewed.
- Thread: 01a0a5cb-8967-7361-a7f6-fc1a0ac3a058. UTC scenario first visible text
  3958 ms; total 4153 ms. Tool times: utc_now 2.63 ms, okf_index 6.75 ms.
  Single smoke, not a latency improvement/capacity benchmark.
- Ten prior local protocol browser tests cover the unchanged application sources;
  this rollout reran the published smoke, not the entire financial Qwen battery.
- Legacy institutions casing remains visible, intentionally not silently renamed.
  RAW instructions govern future ingestion; existing paths are identities.
- The eight-file synthetic financial policy remains a separate draft/example.
  No financial publication or agreement was executed in this live smoke.
- Numeric review happens after text display and does not certify semantic fidelity.

Rollback: preserve /data and server LLM variables; restore previous successful
images (runtime d7a4d82d-46e6-4007-8073-28e084ee5f62, frontend
4e662b1a-1927-47cc-aeae-5f9b8b4be0c2). Preserve exec/no-reload and graceful drain.
The previous frontend image lacks the newly built connection defaults; use its
explicit runtime link until rebuilt. Never restore old data over current sessions
without reviewing the backup and intervening activity.
